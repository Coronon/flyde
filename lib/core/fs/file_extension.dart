/// A class which stores the most common file extensions as static properties.
abstract class FileExtension {
  /// A list of standard extensions for C++ header files
  static const headers = ['.h', '.hpp', '.hh', '.h++'];

  /// A list of standard extensions for C++ source files
  static const sources = ['.c', '.cpp', '.cc', '.c++'];
}
